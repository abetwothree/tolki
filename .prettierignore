# Exclude GitHub workflow files from automatic formatting/commits
.github/workflows/

# (Optional) add coverage output and build artifacts if needed
coverage/
dist/
node_modules/# Ignore artifacts:
build
coverage

# Captured PHP parity transcripts must stay byte-identical to PHP's own
# JSON_PRETTY_PRINT output (see scripts/php-parity/README.md) so a Laravel
# upgrade shows up as a real diff, not a Prettier reformat.
docs/php-parity/